{
  "name": "star-trail-adventure",
  "version": "1.0.0",
  "description": "A polished 3D platformer with world maps, branching paths, and secrets",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "three": "^0.169.0"
  },
  "devDependencies": {
    "@types/three": "^0.184.0",
    "typescript": "^5.6.3",
    "vite": "^6.0.3"
  }
}
